import * as authActions from './auth'
import * as queryActions from './query'
import * as storageActions from './storage'
import * as connectionActions from './connection'

export { authActions, queryActions, storageActions, connectionActions }
export default { authActions, queryActions, storageActions, connectionActions }
